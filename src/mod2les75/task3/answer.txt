Вопросы:
Посмотрите какой номер и как называется ваша версия jdk.
- версия javac 11.0.9

1) Сколько раз выводится сообщение в методе finalize?
- невыводятся, лишь
Iteration 0
Iteration 1

2) Если увеличить n до 10_000_000, будут ли выводится сообщения?
- Да, выводятся
Finalize 10000001...
Process finished with exit code 130

3) Верните n исходное значение и в конец тела метода main добавьте следующие строчки:
      System.gc();
      System.runFinalization();
  Сколько раз выводится сообщение в методе finalize?
-  20раз (2 массива по 10 элементов, то есть для каждого элемента)

4) Посмотрите в документации что делают эти вызовы.
Будут ли выводиться сообщения, если закомментировать одно из них?
Можно ли гарантировать, что метод finalize будет вызываться и когда?

- gc-запускает сборщик мусора
  runF.. - запускает метод финилизации для объектов подлежащих удалению
  Если закомментировать сборщик мусора, то сообещния не выводятся,
  если закомментировать runFinalization, то вывелись сообщения finalize только для второй
  итерации.

  Как понял, если указать  System.runFinalization(), то finalize будет вызываться для всех
  удаляемых объектов, а без него не для всех

5) Попробуйте вновь увеличить n до 10_000_000 и переместить строчки,
добавленные на шаге 3, в конец тела цикла по l. По очереди закомментируйте их
и посмотрите, будут ли печататься сообщения из finalize.

- после добавления в конец цикла строк кода. Вызвался метод Finalize ля всех
элементов итерации 0. Очень долго думала Идея и потом снова по итерации 2. Вызвался
метод Finalize ля всех элементов итерации 1.
Программа завершена - Process finished with exit code 0.

Закомментировал gc. Для первой итерации не вызвался Finalize, для второй вызывается
для всех элементов. Process finished with exit code 0.
Закомментировал System.runFinalization(). Результат как и при комментировании gc

6)Далее установите n, l и m исходные значения, уберите добавленные на прошлом шаге строчки.
  Далее запускайте программу, увеличивая l в 2 раза после каждого запуска, и отследите следующие значения какие в среднем получаются значения
  1) с какого значения l начинает печатать сообщение из метода finalize.
  2) Номера каких элементов начинают печататься? В каком порядке печатаются номера элементов?

- так и не началась печать сообещний при l= 512